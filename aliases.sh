#!/bin/bash

# exec
alias docker-compose-exec='docker compose exec'
alias dce='docker compose exec'

# php
alias docker-compose-exec-php='docker compose exec php'
alias dce-php='docker compose exec php'
# nginx
alias docker-compose-exec-nginx='docker compose exec nginx'
alias dce-nginx='docker compose exec nginx'
# mysql
alias docker-compose-exec-mysql='docker compose exec mysql'
alias dce-mysql='docker compose exec mysql'
# redis
alias docker-compose-exec-redis='docker compose exec redis'
alias dce-redis='docker compose exec redis'

# run
alias docker-compose-run='docker compose run --rm'
alias dcr='docker compose run --rm'

# composer
alias docker-compose-run-composer='docker compose run --rm composer'
alias dcr-composer='docker compose run --rm composer'
# npm
alias docker-compose-run-npm='docker compose run --rm npm'
alias dcr-npm='docker compose run --rm npm'